# Manual line-by-line audit findings

Started 2026-08-18. Every line of every production file under `src/` read by hand.
Not committed. Not generated by a script.

Progress marker lives at the bottom of this file.

---

## Findings carried over from the first pass (exporters.ts, ipc7351.ts)

"(new code)" = written 2026-08-17, i.e. the newest code was the least reviewed.

### Wrong copper, silent

**F1.** `landSpanCrossMm: null` treated as "square" rather than "unknown", and
absent from `RequiredInput.field`, `SuppliedDimensions` and `withSupplied`, so a
rectangular quad can never be completed by the user. NULL-AS-DEFAULT. (new code)

**F7.** The `drawnPackages` match WRONGLY REFUSES parts whose `packageType` looks
like `CFP (14)`: `designatorToken` returns null because the parenthesised token
starts with a digit, and there is no substring hit against `HBH0014A`. Real parts
lost. (new code)

**F17.** `computeLandPattern` (ipc7351.ts) never sets `padCentreCrossMm`, so a
rectangular quad built from the package DRAWING is still placed square. The
rectangular fix covered only the printed-land path. (new code)

### Checks that cannot fire, or are defeated

**F2.** `withinIpcBand` checks only the main span; the cross axis is unchecked. (new code)

**F9.** `withinIpcBand` calls `leadFromDrawing(part)` with no formed span/contact,
so a `straight` lead-form part never gets the band check even after the user
supplies both numbers.

**F15.** `packageOptions`/`optionFor` never receive `supplied` or
`formedLeadContactMm`, so the chooser reports `needs-input` for parts that would
ship. Defeats the anti-drift purpose `optionFor` documents for itself.

**F16.** `optionFor` passes `drawingIsThisPackage: false`, then `asPackage`
short-circuits on `designator === part.packageType` and returns the record
unchanged, keeping another package's dimensions.

### The unanswerable question

**F8.** `askForLandPattern` offers `leadSides` as "(2 or 4)". 1 became valid on
2026-08-17. The through-hole path asks it correctly. FIXED IN ONE PLACE, NOT THE
OTHER. (new code)

### Invalid or misdescribed output

**F4.** STEP `LINE` references VERTEX_POINT ids (20-27) where the schema requires
CARTESIAN_POINT (10-17).

**F5.** STEP `EDGE_LOOP` references EDGE_CURVE directly; AP214 requires ORIENTED_EDGE.

**F12.** `provenance.centreToCentreMm` and the footprint description record only
the main span, so a rectangular quad's own file misstates its geometry. (new code)

### Comment/code drift

**F3.** `stepString` comment claims control characters are stripped; only `\r\n\t`
are, and backslash (STEP's own escape introducer) is not escaped.

**F6.** The symbol-grid comment cites pins at 5.08/2.54/-2.54/-5.08; the code
produces 2.54/0/-2.54/-5.08 for a 4-row symbol.

**F10.** The thermal-pad designator lookup (`part.pins.find(p => !/^\d+$/.test(p.number))`)
is DEAD: `normalizeModelPins` strips non-numeric rows before they reach `part.pins`.

**F11.** "A quad package is square by construction here" is now false, and
`land.gMinMm` (main axis only) is the silkscreen fallback for both axes. (new code)

**F13.** `pin1Marker` y uses `rowSpanMm`, the WIDEST side, not the side pin 1 is
on. Misplaced on a quad whose first side is not the widest.

**F14.** `withSupplied` is applied twice (createExportZip and buildFootprintGeometry)
while the comment claims "applied ONCE, here".

---

## Findings from this pass

(appended as found)

---

## Progress

| file | lines | status |
|---|---|---|
| src/lib/exporters.ts | 2436 | DONE (first pass) |
| src/lib/ipc7351.ts | 496 | DONE (first pass) |

### src/lib/types.ts (898 lines) - READ

**F18. `drawnPackages` is missing from `partSchema`.** It is on the TypeScript
`PartRecord` and `ResolvedPart` types and on the prompt, merge and exporter, but
not on the zod schema. `/api/export` validates the posted record with
`partSchema.safeParse` (route.ts:54) and zod strips unknown keys, so the field is
dropped on the export route: the drawing-match guard in exporters.ts:752 then sees
`undefined` on every export. This is the EXACT defect the `pinTablesByPackage`
comment in the same file documents as already learned and warns against. (new code)
Shape: we had it and threw it away / fixed in one place not the other.

**F19. There is no cross-axis lead span.** `leadSpanMm` is one min/max pair. A
rectangular four-sided package prints TWO spans on its drawing (across D and
across E). Storing one means a rectangular quad can never be built rectangular
from the drawing path at all: this is the root cause behind F17, which cannot be
fixed without the input existing. Same shape on `leadContactMm` is acceptable
(one lead form all round) but the SPAN is per axis by definition.
Shape: assumed square, stored one value.

**F20. Orphan comment.** types.ts:592 `/** One field, read two ways. Both sides
carry the page so a reviewer can check both. */` documents no declaration; the
type it described is gone. Comment/code drift.

### src/lib/confidence.ts (545 lines) - READ

**F21.** `printedPatternInBand` takes `densityLevel` and uses it ONLY in the
"unavailable" message (`density ${densityLevel}`). The check itself computes the
A..C band and ignores the argument, so the message tells a reviewer a density
level was applied that was not. Comment/message asserting behaviour the code lacks.

**F22.** Every reviewer check on the land pattern reads the MAIN axis only:
`landsClearTheCentre` (landSpanMm), `printedPatternInBand` (landSpanMm). On a
rectangular four-sided package the cross axis is unchecked, so opposing lands on
the other pair of sides can overlap and no check reports it. Same family as F2.
(`geometryViolations` does catch the overlap at the output, so this is a
reviewer-visibility defect, not silent wrong copper.)

### src/lib/geometry.ts (192 lines) - READ

**F23.** `FootprintProvenance.centreToCentreMm` is a single number with no cross
axis, so the provenance block a reviewer reads misstates a rectangular quad.
(Same defect as F12, recorded here because the TYPE is the reason it cannot be
stated correctly.)

**F24.** `Pad.pasteApertures[].centre` has no stated frame of reference.
`geometryViolations` treats it as relative to the pad centre; an emitter that
read it as absolute would place paste off the board and nothing would say so.
Documentation gap on a field two files must agree about.

### src/lib/vendorland.ts (226 lines) - READ

**F25.** `landDisagreements` checks `computed.padCentreMm * 2` only. The cross
axis of a rectangular package is never compared against the printed page, so the
vendor-land veto that exists to catch a wrong lead form cannot see a wrong cross
span. Same family as F2/F22.

### src/lib/packagedrawing.ts (242 lines) - READ

**F26. Word-boundary trap in the drawing ranking.** `findPackageDrawing` builds
`new RegExp("\\b" + family + "\\b")` from `packageFamilies(packageType)`, which
returns whole tokens including their digits (`LQFP64`). A page that prints
`LQFP-64` or `LQFP 64` therefore fails `namesPackage`, and the ranking falls back
to raw value count, which is measured to pick the WRONG package's drawing (the
TLV9061 case the comment above it describes). This is the `\bLQFP\b` trap
LEARNINGS records, inverted: here the needle carries the digits and the page does
not. Coverage/correctness loss with the information present on the page.

### src/lib/extraction/merge.ts (852 lines) - READ

**F27. Aspirational comment: the conflict record does not exist.** The block at
merge.ts:655-663 states "A displaced reading becomes a conflict, and each side is
recorded under its OWN name: the code's displaced value in `deterministic`, the
model's new one in `model`, and `holding: \"model\"`". No such code exists in the
function: `alreadyAnswered` early-continues, so nothing is ever displaced and no
conflict is ever written. A reader is told a review signal is produced that is
not. Same shape as the types.ts deadlock comment LEARNINGS section 8 names as the
most likely failure of a doc. Pairs with F20 (the orphan `One field, read two
ways` comment in types.ts, which is the type this comment refers to).

**F28. The TI-numbered thermal pad reclassification updates `pins` and not
`pinCount`.** merge.ts:717-737 removes the last row from `merged.pins` when it is
proved to be the exposed pad, and leaves `merged.pinCount` untouched. If the model
counted that row (pinCount 9 on an 8-lead PowerPAD SOIC), the record now says 9
pins with 8 rows: `buildFootprintGeometry` places pads from `pinCount`, so nine
lead lands are laid on an eight-lead package. Fixed in one place, not the other.

### src/lib/extraction/contracts.ts (355 lines) - READ

No new finding. Confirms `dimensions.landSpanCrossMm` IS asked for, so F1's
null-as-square is purely a consumption defect, and confirms there is no
`leadSpanCrossMm` to ask for at all (F19).

### src/lib/extraction/models/prompt.ts (533 lines) - READ

**F29. The fence-token neutralisation is undone by the line below it.**
`neutralizeUntrustedText` inserts U+200B into `<<<` and `>>>` to break them, and
the NEXT `.replace` strips the character class `[U+202A-U+202E, U+2066-U+2069,
U+200B-U+200F, U+FEFF]`, which contains U+200B. Verified by running the four
replacements: `"<<<"` comes out as `"<<<"`, unchanged. The generic triple-angle
break has never done anything, while the comment above the function states it
does. Guard that cannot fire + comment asserting behaviour the code lacks.
Order the strip BEFORE the insertion and it works.

**F30. `sanitizePartNumber` is applied to PACKAGE DESIGNATORS and destroys
them.** `buildPrompt` runs it over `request.packageType` and every entry of
`request.packageCandidates`. It keeps only `[A-Za-z0-9-._/+]`, so `SOIC (D)`
reaches the model as `SOICD`, `CFP (14)` as `CFP14`, `PDIP (N)` as `PDIPN`. The
model is then asked to match a designator the document does not print, and the
value it returns for `packageType` is compared downstream against the real one.
`vendorland.ts:forRegex` solves the identical untrusted-input problem by
ESCAPING and says in its own comment why stripping is wrong. Fixed in one place,
not the other. Coverage loss with the information present.

**F31. An unusable answer shape is dropped with no trace.** In
`parseModelResponse`, a value that is object-shaped but not a range hits
`continue` and is recorded nowhere: not in `values`, not in `declined`, not in
`notes`. This is the same defect that was fixed for BARE answers three
paragraphs above ("dropped on the floor without a trace"), left on the
neighbouring branch.

**F32.** `coercePinRows`'s comment says the strict reader runs in merge.ts "when
one of these is actually selected". Since 2026-08-16 merge runs it at STORE
time. Stale comment.

### src/lib/extraction/run.ts (223 lines) - READ

**F33. `combine` drops `declined`, so the diagnostic that cost a whole
investigation is dead in production.** `ExtractionResult.declined` exists
specifically to tell "the model looked and the document is silent" from "nobody
asked", and its doc comment names the leadForm investigation it was built after.
`combine` returns `{values, notes, pinTablesByPackage, drawnPackages, usage}`
with no `declined`, so `mergeModelValues`'s `result.declined ?? []` is always
empty on the real pipeline and the note is never written. Only a direct
merge call (tests) ever sees it. We had it and threw it away.

**F34. `combine` drops `attempts`.** Same object, same line. `attempts` exists
because retries were billed and not counted, a measured 40% spend under-report.
Every run through `runExtraction` loses the count for both passes. (Consumers to
confirm when reading budget.ts and the benches.)

### src/lib/extraction/request.ts (224 lines) - READ

**F35. Pass 2 is never told the package pass 1 settled on.** `withRenderedPages`
spreads the pass-1 request, so `packageType` is still whatever the RECORD held
(null unless the user named one), even though pass 1 has just answered it. Pass 2
sees only the rendered drawing pages, with no whole-document context and no
package, which is exactly the state the contract documents as producing a total
refusal ("asked about an LM358 without it, the model correctly returned NULL for
every dimension and said why"). Pass 2 is the pass that reads every land and
drawing dimension. We had the answer and did not carry it forward.

### src/lib/extraction/{untrusted,budget,sections,factory,index}.ts - READ

No findings.

### src/lib/extraction/models/{transport,gemini,vertex,local}.ts - READ

No findings. `isTransient` correctly lands the unknown case on the safe side
(unknown message is not retried).

### src/lib/extraction/models/local-focused.ts (264 lines) - READ

**F36. The ungrouped-field bucket picks the LONGEST page, which is the exact
failure this model exists to avoid.** `groupsFor` sweeps every field not named in
`FIELD_GROUPS` into one group with `locate: /(?:)/`. An empty regex with the `g`
flag matches at every character position, so `pagesFor` scores that group's hits
as the page's LENGTH and sorts descending: the longest page in the document wins.
The file's own comment two lines below says "Send the shortest few rather than
the first few: a wrong LONG page is what produces confident nonsense."

**F37. `FIELD_GROUPS` is an allowlist that the last six months of new fields fell
out of.** Ungrouped today: `landSpanCrossMm`, `solderMaskExpansionMm`,
`solderMaskDefined`, `thermalViaDiameterMm`, `thermalViaPitchMm`, `leadSides`,
`leadForm`, `mounting`, `leadDiameterMm`, `vacantLeadSlot`, `leadsPerSide`,
`jedecOutline`, `packageOutlineCode`. All thirteen are asked as ONE wide question
over ONE wrong page, which is the wide-question failure this model was built to
replace, at scale. Allowlist broken by the next case.
(Local path only, `FORGE_LOCAL_FOCUSED`, so it does not affect the hold-out.)

**F38.** `FocusedLocalExtractionModel.extract` returns no `declined` and no
`notes`, so on the local path the "the model looked and the document is silent"
signal does not exist at all. Same family as F33.

### src/lib/provenance.ts, readout.ts, datasheet.ts - READ

**F39.** `datasheet.ts:185` still writes `unknown<2 | 4>()` for `leadSides`. The
record widened to `1 | 2 | 4` on 2026-08-17. Harmless today (the value is null)
and it is the fingerprint of the same half-applied change as F8. Low.

### src/lib/review.ts (299 lines) - READ

**F40. `REVIEWABLE` is an allowlist and three copper-placing fields are not in
it.** Missing: `dimensions.landSpanCrossMm` (the cross-axis pad centre span, the
whole point of the rectangular-quad work), `dimensions.vacantLeadSlot` (which
grid position carries no lead: wrong means a miswired board that looks right),
`dimensions.leadsPerSide` (which side gets the short row). All three are model
answers that place copper, and none can be reviewed or confirmed, so an uncited
one blocks the export with no way to clear it. This is the same list-versus-
enumeration mistake `untraceableDimensions` in types.ts was rewritten to remove,
and the file even exports `REVIEWABLE_FIELDS` "for tests that check nothing is
missed". Allowlist broken by the next case.

### src/lib/packagevariants.ts (565 lines) - READ

**F41.** The doc comment describing `outlineCodeDesignator` ("The package code a
vendor outline-drawing number begins with") sits immediately above
`normaliseForMatch`'s own comment, so it documents the wrong function; the real
`outlineCodeDesignator` is declared below with no comment. Comment/code drift.
Low.

### src/lib/{pdftext,fontdecode,pagerender}.ts - READ

No findings.

### src/lib/preflight.ts (127 lines) - READ

**F42. Preflight does not know Vertex exists.** `reportPreflight` logs
`modelConfigured: Boolean(GOOGLE_GEMINI_API_KEY || FORGE_LOCAL_MODEL_URL)`, so a
Vertex-only deploy, which the factory PREFERS over the API key, reports
`modelConfigured: false` in the one log line whose job is to say what the process
will do. Check 2 (`CLOUD_KEY_IN_AIRGAP`) has the same gap on the safety side: an
air-gapped deploy carrying `GOOGLE_APPLICATION_CREDENTIALS` and
`FORGE_VERTEX_PROJECT` gets no warning at all. This is the same allowlist-of-
known-providers shape as the `wasPaidFor` billing hole that disabled the spend
ceiling, in the same product, after that lesson was written down.

### src/lib/emitters/kicad.ts (491 lines) - READ

**F43. Two consumers disagree about the frame of `pasteApertures[].centre`.**
`emitKicadFootprint` writes it as an ABSOLUTE footprint coordinate
(`(at ${aperture.centre.xMm} ...)`), while `geometryViolations` in confidence.ts
tests `Math.abs(aperture.centre.xMm) + widthMm/2 <= pad.widthMm/2`, i.e. as a
coordinate RELATIVE to the pad centre. The two agree only because every exposed
pad this generator builds is centred on the origin. An off-centre pad makes one
of them silently wrong, and the invariant is the one that would stop noticing.
Upgrades F24 from a documentation gap to a live disagreement.

**F44.** `fabricationOutline`'s comment says "Pin 1 sits at the top of the
left-hand side on both arrangements this generator builds". There are three
arrangements now (`leadSides` 1, 2 and 4). Comment drift, same half-applied
change as F8 and F39.

### src/lib/emitters/altium/pcblib.ts (927 lines) - READ

**F45. A stale refusal comment at the top of `emitAltiumPcbLib`.** Lines 569-574
say "A pad whose paste must not follow its copper cannot be spelled in this
writer yet ... Refusing is the only honest option, because the file would look
correct in Altium and fail at reflow." There is no refusal below it, and there
should not be: the writer now suppresses paste on the copper land and emits the
apertures on Top Paste. A reader is left believing a guard exists. Delete the
claim.

**F46. Confirms the direction of F43.** `emitAltiumPcbLib` passes
`centre: aperture.centre` straight into `padRecord`, which writes it as an
absolute footprint coordinate. Both emitters therefore treat the field as
absolute, so `geometryViolations`'s relative interpretation is the wrong one, and
the paste-inside-copper invariant is only accidentally correct.

**F47. The silkscreen clipping algorithm exists twice, ~60 lines each**, in
kicad.ts (`silkscreenOutline`) and pcblib.ts (`silkscreenTracks`): same blockers,
same `edge`, same `survives`, same `clearOf`, same constants derived two ways.
The KiCad copy carries a comment recording that the `clearOf` candidate names
"read correctly for a negative edge and backwards for a positive one" and were
renamed; the Altium copy still calls them `inward`/`outward` with the old
backwards meaning. Behaviour is identical today. This is the dominant failure
shape in this codebase sitting in the emitters, pre-loaded.

### src/lib/emitters/altium/{units,binary,layers,templates,container,schlib}.ts, emitters/altium.ts - READ

No findings.

### src/app/api/{config,parse,export,lookup}/route.ts (815 lines) - READ

**F48. Both routes render the model's pages TWICE.** `runExtraction` renders the
pages inside `withRenderedPages` and then returns only their page NUMBERS
(`ExtractionRun.renderedPages: number[]`), so `/api/parse` and `/api/lookup` each
call `renderPages` again on the same pages for the review panel. Both sites carry
a comment saying "re-rendering a page we already rasterised for the model would
be pure waste" immediately above the second render. We had the images and threw
them away, and it costs the route's own 30s budget on every part.

**F49. `/api/lookup` runs the model with NO time budget.** `/api/parse` computes
`modelBudgetMs`, checks `worthAsking` and wraps the call in `withDeadline`,
because a model call can take 41.6s against a 30s `maxDuration` and being killed
by the platform loses a record that already succeeded. `/api/lookup` has the same
`maxDuration = 30`, does MORE work first (network retrieval of the PDF), and
calls `runExtraction` unwrapped. The exact failure `budget.ts` exists to prevent,
on the route most likely to hit it. Fixed in one place, not the other.

**F50. `/api/export` rejects `leadSides: 1`.** Line 179: `if (sides !== 2 && sides
!== 4)`. `1` became a valid reading on 2026-08-17 specifically so a TO-220, TO-92
or SIP could be represented. A user whose datasheet does not state it therefore
cannot supply it, and the export refuses. This is the THIRD copy of the same
enum question (record schema, prompt, `askForLandPattern`, this validator) and
the second one that was not updated. Extends F8.

**F51. `suppliedNumbers` omits `landSpanCrossMm`.** Confirms F1 at the route: the
field the rectangular-quad work added is not in the accept list, so it can never
be supplied.

**F52. `leadsPerSide` is accepted only as EXACTLY four counts** (`^\d{1,3}(,\d{1,3}){3}$`),
while the record's own schema is `^\d+(,\d+)*$`. Verify against what
`askForLandPattern` actually asks for before treating this as live.

**F53. Both routes report `method: "deterministic"`.** The deterministic parser
was deleted on 2026-08-14; what remains fills the part number from the FILE NAME
and the package from the USER. The comments above it ("Deterministic pass ALWAYS
runs first and always wins. A model is only ever asked about fields the code
could not read off the page") are the stale README claim CLAUDE.md already names,
reproduced in product code and shown to the user as the extraction method.

### src/app/page.tsx (1216 lines) - READ

**F54. `asPackage` NEVER RUNS ON THE EXPORT PATH.** `/api/export` calls
`asPackage` only when the request body carries a `packageType`, and the UI never
sends one: `handleExport` posts `{ part, format, ...answers, ...install }`.
`handlePackageChoice` instead writes the new designator into the RECORD
(`packageType: userEdited(designator)`) in two of its three branches, and in both
it leaves `dimensions.*` as they were read for the PREVIOUS package. So choosing
a sibling package in the product produces a record whose name is one package and
whose body, lead span, land pattern and outline code are another's, and every
guard `asPackage` documents itself as carrying (blank the dimensions, take that
package's pin table, the lead-count check) is bypassed. LEARNINGS calls this
exact shape "the worst failure this product has, because every input is
individually valid and the result looks entirely ordinary in CAD". The chooser
PREVIEW (`packageOptions` -> `optionFor` -> `asPackage`) does run it, so the
button's own label is computed under rules the export does not apply.

**F55. The record panel shows 15 of 25 dimensions and calls itself "every
dimension".** Absent: `landSpanCrossMm`, `thermalPadWidthMm`, `leadsPerSide`,
`vacantLeadSlot`, `leadDiameterMm`, `solderMaskDefined`, `thermalViaPitchMm`,
`leadCount`, `leadLengthMm`, `mounting` (in the record table). Two matter most:
`thermalPadLengthMm` is shown alone under the label "Exposed pad", so a
rectangular pad reads to a reviewer as one number; and `landSpanCrossMm`, the
field the rectangular-quad work exists for, is invisible, so a wrong or missing
cross span can never be seen or corrected. Same allowlist shape as F40.

**F56.** `defaultPart` (page.tsx:89) declares `leadSides: nothing<2 | 4>()`.
Same stale enum as F39.

**F57.** `MAX_LEAD_SPAN_MM = 200` is documented as "Largest span the export route
accepts, mirrored so the UI refuses it first" and is applied to EVERY mm answer,
including `formedLeadContactMm`, which the route caps at 5. The UI accepts 8 and
the route returns 400. Mirrored in one place, not the other.

### src/lib/retrieval/** (2,591 lines, excluding __bench__) - READ

**F58.** `scrape.ts:inspectCandidate` fetches EVERY pdf link `extractPdfLinks`
finds on a third-party HTML page, unbounded, each with a 30s download timeout.
The search-result list is capped at 12 candidates but the per-page link list is
not, so one hostile or merely link-heavy page turns into an unbounded outbound
fan-out from a public endpoint. Everything else on this path is bounded
deliberately (redirects at 5, body at 50MB, chain at 12s). Low, and the same
class the file's own header worries about.

No other findings. The SSRF guard, the pinning, the composite's soft/hard split
and the cache all read correct.

### src/lib/__bench__/{env,packagehint,modelcache}.ts - READ

**F59.** `packagehint.ts:49` calls
`buildPartRecord(doc, "${partNumber}.pdf", partNumber, hint ? {...})`. The third
parameter of `buildPartRecord` is `sourceUrl`, not a part number, so the probe
stores the part number as the record's source URL. It is a probe, so the harm is
cosmetic, but it means the instrument is not building "exactly what the route
builds" as its own comment claims.

modelcache.ts (978): no findings. The billing inversion, the ledger, the pacing
and the pre-run projection all read correct and each carries the measurement
behind it.

### src/lib/__bench__/extraction.ts (829 lines) - READ

**F60. The bench scores the dimensions that USED to place copper.**
`SCORED.geometry` is `["dimensions.bodyLengthMm", "dimensions.bodyWidthMm",
"dimensions.pitchMm"]`. Since 2026-08-12 the pads come from `landPadLengthMm`,
`landPadWidthMm`, `landSpanMm` (+ `landSpanCrossMm`, `leadSides`, `leadForm`,
`mounting`), and none of the seven is scored. So the `geom` column and every
"fields complete" figure quoted from this bench describe three fields that no
longer build the footprint. This is verbatim the defect `untraceableDimensions`
in types.ts was rewritten to remove, with the same lesson written above it,
surviving in the instrument that measures the fix. Allowlist + fixed in one place
not the other.

**F61.** `extraction.ts` ends with a bare `main();` and no `.catch`, unlike
`coverage.ts`. A throw inside becomes an unhandled rejection with no report.

### src/lib/__bench__/holdout.ts (541 lines) - READ (runner only; no part was opened)

**F62.** `shipOutcome`'s route two calls `packageOptions(record)`, i.e. it counts
a part as SHIPPING when a sibling package would build under `asPackage`'s rules.
The product's own export path never applies those rules (F54), so the headline
SHIPS figure describes a path the user does not have. Fixing F54 is what makes
this measurement true.

**F63.** `holdout.ts` and `extraction.ts` both end with a bare `main();` and no
`.catch`. On the PAID run that means a throw anywhere outside the two guarded
try blocks becomes an unhandled rejection: money spent, no summary printed. This
is the same shape as the `shipOutcome` rethrow that killed a 56-part run on
2026-08-17, one level up.

### src/lib/__bench__/pinout-oracle.ts (691 lines) - READ

**F64. The ceramic guard the oracle reports against no longer exists.**
`PackageOracleEntry.ceramic` and `checkPackageFamily`'s `ceramicLost` are both
documented as protecting the guard in `packages.ts` that "refuses a ceramic part
the plastic JEDEC families by testing the designator string" for the word.
`src/lib/packages.ts` was deleted, and no product file tests a designator for
`ceramic` or `hermetic` any more (checked across src/lib and src/app). The same
stale rationale is carried by `MATERIAL_QUALIFIER`/`MATERIAL_WORD` in
packagevariants.ts, whose comment says "the guard in `packages.ts` that keeps a
hermetic part off plastic JEDEC geometry tests the designator STRING". So the
bench prints `CERAMIC LOST` for a consequence that cannot occur, and a reader of
either file believes a guard exists. Keeping the printed qualifier is still
right (it is what the vendor wrote and it goes into the prompt and the footprint
name); the CLAIM about why is false and must be corrected.

### src/lib/__bench__/{dimensions,dimension-oracle,mutation,guards}.ts - READ

**F65. `guards.ts:154` reads the MAIN span into the CROSS span.**
`landSpanCrossMm: asNumber(at("dimensions.landSpanMm"))`. Every part the guard
bench builds is therefore a SQUARE quad by construction, whatever its document
says, so the bench cannot see a rectangular package at all and would report a
cross-axis defect as healthy. Copy-paste, and it is the exact shape this audit
was called for.

**F66.** `guards.ts:155` `leadSides: sides === 2 || sides === 4 ? ... : null`
drops a legitimate `1`. `dimension-oracle.ts` types `leadSides?: 2 | 4` for the
same reason. Same stale enum as F8/F39/F50/F56.

**F67.** `dimensions.ts` (the correctness bench) compares `land.padLengthMm`,
`padWidthMm` and `spanMm` and has no cross-span field, so no hand-read entry can
ever check a rectangular quad's second axis. `DIMENSION_ORACLE.land` has no key
for it either.

mutation.ts: no findings. NOTE FOR THE FIX PHASE: several mutations match exact
source lines in `ipc7351.ts`, `exporters.ts`, `kicad.ts` and `pcblib.ts`. Any fix
that rewrites one of those lines turns the mutation into NOT APPLIED, which is a
hole that still looks like a member of the set. Re-run `npm run bench:mutation`
(free) after the fixes and repair every NOT APPLIED the run it is reported.

### src/lib/__bench__/replay.ts (255 lines) - READ

**F68. The same cross-span bug as F65, in a second copy.** `replay.ts:151`
also reads `landSpanCrossMm: asNumber(at("dimensions.landSpanMm"))`. Both
`replay.ts` and `guards.ts` carry a near-identical ~60-line `partFrom` that
builds a `ResolvedPart` out of a cached answer, and both carry the same two
defects (cross span copied from the main span, `leadSides` 1 rejected). Two
copies of one rule, both broken the same way, which is what the shape guarantees.

---

## READING COMPLETE

Every production file under `src/` has now been read line by line except
`exporters.ts` (2,436) and `ipc7351.ts` (496), which were read in the previous
session and produced F1-F17. Those two are re-read during the fix phase.

---

## FIXES APPLIED (2026-08-18)

Every fix is stated without naming a part or a vendor. Uncommitted.

### The rectangular-quad chain, end to end (F1 F2 F11 F12 F17 F19 F22 F23 F25 F51 F55 F65 F67 F68)

- `types.ts` gains `dimensions.leadSpanCrossMm`, the drawing's SECOND span. A
  rectangular quad's outline prints both and the record carried one, which is
  why F17 could not be fixed: the input did not exist.
- `contracts.ts` and the prompt now ask for it, with the axis stated.
- `ipc7351.ts:computeLandPattern` takes `spanCross` and sets `padCentreCrossMm`
  from the SAME derivation, so the drawing path places all four sides correctly.
  The courtyard takes the wider axis, matching the printed-land path.
- `exporters.ts:leadFromDrawing` supplies it; `printedLand` reads the cross span
  before the band check; `withinIpcBand` checks BOTH axes and now receives the
  formed span and foot so a straight-lead package is checked at all (F9).
- `landSpanCrossMm` is now ASKED for on a four-sided package when it is null
  (never assumed equal), accepted by `/api/export`, filled by `withSupplied`,
  reviewable in the panel, and shown in the record.
- The silkscreen fallback and the pin-1 marker use the correct axis and the
  correct row (`crossGapMm`, `firstRowSpanMm`).
- Provenance and the footprint description state both spans.
- `landDisagreements` compares the cross axis against the printed page.
- `guards.ts` and `replay.ts` no longer read the main span into the cross span.

### asPackage now runs on the export path (F54)

The UI held the chosen package in `chosenPackage` and sends it as
`packageType`, instead of writing it into the record. The record says what was
READ; the request says what the caller is HOLDING. `asPackage` is the one place
the relabelling rule lives and it now runs on the export, not only in the
chooser that computes the button's label.

### The unanswerable questions (F8 F50 F52 F57 F58-UI)

`leadSides: 1` is offered by `askForLandPattern` and accepted by the route and
the UI. `leadsPerSide` accepts one count per side for the arrangements the
generator builds (1, 2 or 4) in the route, the UI and the record. The UI mirrors
the route's own per-field bound on the formed foot.

### We had it and threw it away (F33 F34 F35 F48)

`combine` carries `declined` and `attempts`. Pass 2 is told the package pass 1
settled on. `runExtraction` returns the renders it already made, and both routes
use them instead of rasterising the same pages a second time.

### Guards that could not fire, or refused what they could not prove (F7 F29 F42)

- The `drawnPackages` match now returns match / mismatch / UNDECIDABLE per
  drawing and refuses only when every drawing is decidable and every one says no.
- `neutralizeUntrustedText` strips zero-width and bidi characters BEFORE
  inserting one as a fence separator. Verified: `"<<<"` now comes out broken.
- Preflight knows Vertex exists, on both the posture line and the air-gap warning.

### Read correctly, then use correctly (F26 F28 F30 F31 F36 F37 F60)

- Package designators are escaped, not stripped, for the prompt.
- The pin count is corrected when a numbered thermal-pad row is reclassified.
- The drawing ranking matches `LQFP-64` and `LQFP 64` against `LQFP64`.
- An unparseable answer shape is recorded as declined rather than dropped silently.
- The focused local model's catch-all no longer takes the longest page, and the
  thirteen fields that had fallen out of its groups are back in real groups.
- The extraction bench scores the fields that actually place copper.

### Comments made true or deleted (F20 F21 F27 F32 F41 F44 F45 F47 F53 F64)

### Bounded (F58 F59 F61 F63)

Scraped pages yield at most 8 PDF links; both money-spending benches report a
throw instead of dying as an unhandled rejection.

---

## MEASURED, so the trades are not reasoned about

**The cross-span ask costs nothing.** `npm run bench:replay` over the 113 cached
model answers, free: five parts gained the `landSpanCrossMm` question and every
one of the five was ALREADY asking for `landPadLengthMm`, `landPadWidthMm` and
`landSpanMm`. Not one part that shipped before stops shipping. What changes is
that a four-sided package with an unread cross span now asks a fourth question on
the same page as the other three, instead of silently placing both axes at the
main span and calling a rectangle a square.

    SHIPS 55/113 (49%)   ASKS 34   REFUSED 5   NO RECORD 19
    asked for: landSpanMm 22, landPadLengthMm 19, landPadWidthMm 19,
               leadSides 12, formedLeadSpanMm 7, formedLeadContactMm 7,
               landSpanCrossMm 5, body* 5 each, vacantLeadSlot 2

**The mutation set still applies.** All 20 mutations match their target line
exactly once after the edits, checked directly rather than by running the bench.

**Suite: 648 tests, 648 pass, 0 fail. Typecheck clean.** Six tests were added
proving the new behaviour fires (rectangular placement, square unchanged, a bad
cross span not refusing the main axis, the fences actually broken, the designator
reaching the model as printed, the catch-all not taking the longest page).

Three existing tests asserted the DEFECT and were corrected, each with the reason
written where the assertion is:
- `leadSides 1 must be rejected` pinned the unanswerable question.
- Two route tests pinned `method` starting with `deterministic`, a label naming a
  reader deleted on 2026-08-14. They now assert the property that matters, which
  is that no cloud model appears in an air-gapped extraction.

### Found while fixing, in exporters.ts

**F69. The generated STEP solid does not close.** Two of the six faces name a
vertical edge belonging to the OPPOSITE side: the -x face carried edge 40, which
joins the two vertices at +x, and the +x face carried 41, which joins the two at
-x. So neither face is a loop and the shell is not a solid. Invisible in the
text and immediate the moment a CAD tool tries to sew it. Found by walking the
loops by hand while fixing F4 and F5.

FIXED together with F3, F4 and F5:
- the two faces name their own edges;
- `EDGE_LOOP` is built from `ORIENTED_EDGE` entities, with the sense DERIVED by
  walking each loop head to tail rather than tabulated, so it cannot drift, and a
  loop that does not close throws instead of writing an invalid body;
- `LINE` references the `CARTESIAN_POINT` its origin is, not the `VERTEX_POINT`
  built on it;
- `stepString` strips the whole C0/C1 range its comment already claimed, and
  doubles the backslash that introduces Part 21's own escape directives.

**F15 FIXED.** `packageOptions` and `optionFor` now take an `OptionAnswers`
object carrying `formedLeadSpanMm`, `formedLeadContactMm` and `supplied`, so the
chooser builds each option with what the caller has already answered. It was
reporting `needs-input` for packages every question had been answered for, which
is the exact drift `optionFor` documents itself as existing to prevent.

**F14 FIXED (comment).** `withSupplied` runs at both entry points and the comment
said "applied ONCE, here". It is idempotent by construction (it fills blanks
only), and `packageOptions` reaches `buildFootprintGeometry` directly with
answers of its own, so both applications are load-bearing. The comment now says
so instead of describing a single call that does not exist.

**F16 examined, not a live defect.** `asPackage` returns the record unchanged
when the designator already matches, and `optionFor` passes
`drawingIsThisPackage: true` exactly when they match, so the short-circuit is
never reached with a stale record. What made it look live was F54: the UI wrote
the chosen designator into the record before exporting, so the two matched and
the rule was bypassed. Fixing F54 removes the condition.

---

## HOLD-OUT RESULT

Two runs (of the four allowed). The second re-ran only the calls the first lost
to a Vertex 429, at $0.13.

    READ    50/54  (93%)
    SHIPS   14/54  (26%)
    cost    $2.07 + $0.13.  CUMULATIVE ACROSS EVERY RUN EVER: ~$16.39

One call still failed on an unrecoverable 429, so both figures are a FLOOR.

### What blocks the parts that read

    16  needs landPadLengthMm, landPadWidthMm, landSpanMm, leadSides
    10  held: missing pinCount, pins   (a pinout per package, the user picks)
     2  needs vacantLeadSlot
     2  invalid: the output invariant refused the footprint
     2  needs landPadLengthMm, landPadWidthMm, landSpanMm, landSpanCrossMm
     2  held: uncitable pins
     1  held: uncitable leadForm, mounting
     1  needs landSpanMm

The bottleneck is the datasheet's printed footprint not being read at all (16),
and the per-package deadlock (10). `landSpanCrossMm` appears on 2 parts and on
both it sits beside the three land numbers they were already missing, so the new
question costs nothing there either.

### What this does NOT establish

The previous figure on record is READ 51/58 (88%), SHIPS 18/58 (31%). It is NOT
a comparable baseline and must not be quoted as one:

- different corpus (59 parts now, 54 cached; 58 before);
- it predates the PREVIOUS session's uncommitted work as well as this one's, so
  a delta cannot be attributed to either;
- one call in this run still failed, so 14 is a floor.

READ moved 88% to 93% and that direction is safe to state. The SHIPS count is
14 against 18 and I cannot attribute the difference from these two runs.

### What IS measured about the new refusal paths

`npm run bench:guards`, free, tuned corpus only, as RULES.md requires:

    printed-outside-ipc-band       never fires
    printed-land-wider-than-pitch  never fires
    printed-rows-overlap           never fires
    lead-too-wide-for-pitch        never fires
    computed-contradicts-printed   never fires
    computed-inconsistent          never fires
    package-name-vs-pin-count      never fires

No guard fires on the tuned corpus, including the band check now that it also
tests the cross axis and now that it can run at all on a straight-lead package,
and including the vendor-land veto now that it compares the second span. Nothing
was newly refused.
